name: motion-levels-controller

services:
  controller:
    image: ${CONTROLLER_IMAGE:-motion-levels-controller:local}
    build: .
    user: "10001:10001"
    init: true
    restart: unless-stopped
    network_mode: host
    environment:
      # Keep local compose runs away from the production floor broadcast.
      MOTION_LEVELS_FLOOR_SOURCE_IP: 127.0.0.1
      MOTION_LEVELS_LED_BROADCAST_IP: 127.0.0.1
    volumes:
      - controller-state:/var/lib/motion-levels/floor-controller
    read_only: true
    cap_drop: [ALL]
    security_opt:
      - no-new-privileges=true
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=16m
    pids_limit: 128
    mem_limit: 256m

volumes:
  controller-state:
